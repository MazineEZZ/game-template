const gameSettings = {
  width: 1280,
  height: 720,
  ratio: 16 / 9,
  margin: 5,
  grid: 80,
  bgColor: "blue",
  keys: {
    move_up: "z",
    move_down: "s",
    move_left: "q",
    move_right: "d",
    pause_game: "p",
  },
};

const playerSettings = {
  speed: 400,
};

export { gameSettings, playerSettings };
